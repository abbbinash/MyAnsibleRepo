---
- hosts: all
  become: yes
  tasks: 
  - name: i want to install webserver on ubuntu
    apt:
     name: apache2
     state: present
    when: ansible_os_family=="Debian" 
  - name: run 
    service:     
     name: httpd
     state: started
    when: ansible_os_family=="Debian"   
